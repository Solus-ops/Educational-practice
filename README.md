# УП 01
## Работа выполняется поэтапно. Репозиторий обновляется по мере исполнения.

Папка screenshot используется для оперативной частичной проверки работы

## Список ошибок:
1. таблица Products пуста, данные остались в промежуточной таблице StgProduct;
2. cхема БД не синхронизирована с EF-моделью: в коде используется столбец Discount, которого нет в базе данных;
3. механизм авторизации не функционирует - нет реальной проверки логина и пароля из БД;
4. система ролей не обеспечена данными;
5. интерфейс не отображает данные из-за пустой таблицы;
6. не применена Icon (ошибка).
